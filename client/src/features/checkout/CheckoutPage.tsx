import { Typography } from "@mui/material"

export default function CheckoutPage(){
    return(
        <Typography variant="h3">
            Oops! - only logged in user can able to see this page
        </Typography>
    )
}